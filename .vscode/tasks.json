{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Main",
            "type": "shell",
            "command": "cd build && cmake -DCMAKE_BUILD_TYPE=Debug ../cpctools && make",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}